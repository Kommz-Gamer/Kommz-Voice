# =============================================================================
#  KOMMZ VOICE — VARIABLES D'ENVIRONNEMENT
#  Fichier : .env  (ne jamais commiter sur Git !)
#  Copier ce fichier en .env et remplir les valeurs réelles.
# =============================================================================

# ----------------------------------------------------------------------------
# FLASK
# ----------------------------------------------------------------------------
# Clé secrète Flask — générer avec : python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=184b77c6aa621a70934867eb3591ad9f2e4859bafb5076fd09e95097229edb36

# Mode d'exécution : development | production
FLASK_ENV=production

# Port d'écoute (Render.com injecte automatiquement PORT)
PORT=5000

# ----------------------------------------------------------------------------
# SUPABASE
# ----------------------------------------------------------------------------
# URL du projet : Supabase → Settings → API → Project URL
SUPABASE_URL=https://wmgddrgqrhuwjjynvlfx.supabase.co

# Clé SERVICE ROLE (côté serveur uniquement — accès total, bypasse RLS)
# Supabase → Settings → API → service_role (secret)
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6IndtZ2Rkcmdxcmh1d2pqeW52bGZ4Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjYwNTQ3NzMsImV4cCI6MjA4MTYzMDc3M30.FLM-Mkuh0lgTbruqWCHOhnmjDsWurbJInZ8hAhi99jg

# Clé ANON (si besoin côté client — lecture publique uniquement)
# SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.XXXXXXXX...

# ----------------------------------------------------------------------------
# MODAL.COM — Endpoints
# ----------------------------------------------------------------------------
# URL du déploiement Whisper (après : modal deploy modal_whisper.py)
MODAL_WHISPER_URL=https://rexxmoun4455--kommz-whisper-transcribe.modal.run

# URL du déploiement XTTS v2 (déjà déployé depuis les sessions précédentes)
MODAL_XTTS_URL=https://rexxmoun4455--diamond-generate.modal.run

# ----------------------------------------------------------------------------
# SÉCURITÉ LICENCE
# ----------------------------------------------------------------------------
# Salt pour la vérification des clés VCV- (identique dans vtp_core.py !)
VOICE_SECRET_SALT=VTP-KOMMZ-VOICE-2026-X77

# Salt pour le hashage des mots de passe
PASSWORD_SALT=KommzVoiceSalt2026!

# ----------------------------------------------------------------------------
# ADMIN
# ----------------------------------------------------------------------------
# Secret pour accéder aux routes /admin/*
# Utiliser dans le header : X-Admin-Secret: VOTRE_SECRET
ADMIN_SECRET=CHANGE_ME_ADMIN_SECRET_LONG_STRING

# ----------------------------------------------------------------------------
# OPTIONNEL — Notifications email (si tu veux des alertes admin)
# ----------------------------------------------------------------------------
# SENDGRID_API_KEY=SG.XXXXXXX
# ADMIN_EMAIL=admin@kommz.fr
